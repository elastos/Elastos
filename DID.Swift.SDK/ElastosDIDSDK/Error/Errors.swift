import Foundation

class Errors {
    static let CREDENTIAL_ALREADY_SEALED = "VerifiableCredential already sealed."
    static let DOCUMENT_ALREADY_SEALED = "DIDDocument alreay sealed."
    static let PRESENTATION_ALREADY_SEALED = "VerifablePresentation already sealed"

    static let NO_PRIVATE_KEY_EXIST = "No private key"
}
