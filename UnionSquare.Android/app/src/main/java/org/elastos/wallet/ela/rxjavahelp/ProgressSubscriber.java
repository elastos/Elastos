package org.elastos.wallet.ela.rxjavahelp;

import android.content.Context;

public class ProgressSubscriber {
  /*  public ProgressSubscriber(Context context,  viewData, SubscriberOnNextLisenner nextLisenner,
                              boolean isShowDialog) {
        this.progressDialogHandler = new ProgressDialogHandler(context, this, true);
        this.context = context;
        nextLisenner.setViewData(viewData);
        this.nextLisenner = nextLisenner;
        this.isShowDialog = isShowDialog;
    }*/
}
