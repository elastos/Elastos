post:
  tags:
  - task
  description: create task api
  operationId: task/create
  security:
    - api-token: []


  responses:
    200:
      $ref: '../../schema/success_response.yml'
    401:
      $ref: '../../schema/unauthorized_response.yml'

  requestBody:
    $ref: '../../schema/task.yml#create_param'