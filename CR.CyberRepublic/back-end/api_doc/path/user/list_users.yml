get:
  tags:
  - user
  description: get users with userIds
  operationId: user/userIds/users

  responses:
    200:
      $ref: '../../schema/success_response.yml'
    401:
      $ref: '../../schema/unauthorized_response.yml'

  parameters:
    - name: userIds
      in: query
      schema:
        type: string
      example:
