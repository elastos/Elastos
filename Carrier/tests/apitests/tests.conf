@include "carrier.conf"

# Default run test cases in sequence
shuffle = 1

# Default not write log file
log2file = 1

# Optional
tests = {
    loglevel = 4
}

# Optional, Default run on 127.0.0.1:7237
robot = {
    host = "127.0.0.1"
    port = 7237
    loglevel = 4
}

